# Whispy Voice-to-Text Configuration

# Required: Your Groq API key
# Get it from: https://console.groq.com/keys
GROQ_API_KEY=your_groq_api_key_here

# Optional: Output mode (default: both)
# Options:
#   - clipboard: Only copy to clipboard
#   - insert: Only insert at cursor position
#   - both: Copy to clipboard AND insert at cursor
OUTPUT_MODE=both

# Optional: Processing mode (default: improved)
# Options:
#   - instant: Show raw transcription immediately
#   - improved: Wait for AI improvement before showing
#   - both: Show raw first, then replace with improved version
PROCESSING_MODE=improved
